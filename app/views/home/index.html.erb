<% @events.each do |e| %>
<section class="event">
  <div class="summary">
    <h2><%= link_to e.name, e %></h2>
    <h3><%= day_and_time(e) %> in <%= e.location %></h3>
    <span class="price"><%= price(e) %></span>
    <p><%= e.description %></p>
  </div>
</section>
<% end %>